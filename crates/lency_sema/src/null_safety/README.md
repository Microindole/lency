# Null Safety 模块

本目录对 nullable 值执行流敏感检查，跟踪分支和语句执行后的可空状态。

| 文件 | 职责 |
|---|---|
| `mod.rs` | 分析状态、程序入口和合流逻辑 |
| `expr.rs` | 表达式中的 nullable 使用 |
| `stmt.rs` | 语句、分支和循环中的状态传播 |

该模块只处理 `T?` 的空安全，不实现异常传播，也不把普通 enum 当作特殊错误类型。
