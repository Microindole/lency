# Type Check 模块

本目录验证已经解析的 AST 是否满足强静态类型约束。

| 路径 | 职责 |
|---|---|
| `mod.rs` | checker 状态和公共检查入口 |
| `decl.rs` | 函数、类型和实现声明检查 |
| `expr.rs` | 表达式、调用与 enum 构造检查 |
| `stmt/` | 语句和控制流检查 |

普通调用、嵌套调用、函数值调用和 enum 构造必须经过同一套参数数量及类型验证。发现不一致时应返回明确诊断，不得以 `unknown` 静默继续到 codegen。
