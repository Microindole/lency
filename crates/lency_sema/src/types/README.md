# Types 模块

本目录保存语义阶段使用的类型信息和注册表。

| 文件 | 职责 |
|---|---|
| `mod.rs` | 类型模型和公共导出 |
| `info.rs` | 已解析类型的结构化信息 |
| `registry.rs` | 用户类型、泛型和实现注册 |
| `tests.rs` | 类型注册与查询回归 |

源码层 `TypeRef` 属于 `lency_syntax`；这里负责语义身份与约束。不要把 LLVM 表示或 ABI 降级规则放入本模块。
