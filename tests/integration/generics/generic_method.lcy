// @expect-error: FIXME - 泛型方法调用存在 bug ('Box<int>' is not callable)
struct Box<T> {
    T value
}

impl<T> Box<T> {
    T get() {
        return this.value
    }
}

int main() {
    var b = Box<int> { value: 42 }
    var x = b.get()
    print(x)
    return 0
}
