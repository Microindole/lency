// 泛型结构体测试

struct Box<T> {
    T value
}

int main() {
    var b = Box<int> { value: 10 }
    
    // 验证字段类型
    var val: int = b.value
    
    // 错误测试 (应该在语义分析阶段失败，但这里是验证正确性)
    // var s: string = b.value
    
    return val
}
